import React from 'react';
import ReactDOM from 'react-dom';
import App from './app/components/App';

ReactDOM.render(<App />, document.getElementById('mountNode'));
// hydrate require markup in server side
